Frontend (React + Tailwind)

Quick start (local):

```bash
npm install
npm run dev
```

- Configure API base URL to point to backend (e.g., via VITE_API_BASE)
- Implement `services/spotify.ts` to call backend endpoints
- Use React Query for data fetching and cache
